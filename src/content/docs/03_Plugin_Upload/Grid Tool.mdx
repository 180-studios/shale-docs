---
title: Grid Tool
description: Using the ShaleGrid to define a volumetric capture region for environment review.
sidebar:
    order: 5
---

The **ShaleGrid** places capture nodes in a 3D volumetric cage over your scene. When you export, Shale captures a 360° view at each node and assembles them into a navigable environment for your reviewers.

## Adding a Grid

In the **Capture** tab, click **Add Grid**. A cyan wireframe cage (labelled `ShaleGrid`) will appear in your viewport. Move, rotate, and scale it like any other Blender object to position it over the area you want captured.

## Grid Settings

With the ShaleGrid selected, the Capture tab shows its settings:

| Setting | Description |
|---|---|
| **X / Y / Z** | Number of capture nodes along each axis. A 3×3×3 grid places 27 nodes across the volume. |
| **Size** | The physical size of the grid in metres. |
| **Draw Color** | The display colour of the cage in the viewport — doesn't affect the capture. |
| **Capture Distance** | How far from each node the scene is rendered. Default is 100m. |

## Preview

Click **Show Preview** under Capture Preview to see a preview of what will be captured from the current node positions before committing to a full export.

## Exporting

Click **Export Version** to start the capture. Shale renders a 360° view at every node in the grid and uploads them to your linked asset in the Shale portal.
